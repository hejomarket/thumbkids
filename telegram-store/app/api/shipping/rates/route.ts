import{NextResponse}from"next/server";import{z}from"zod";import{getShippingRates}from"@/lib/integrations";const schema=z.object({destination_postal_code:z.number().int().positive(),destination_latitude:z.number().optional(),destination_longitude:z.number().optional(),items:z.array(z.object({name:z.string(),value:z.number(),weight:z.number(),quantity:z.number()})).min(1)});export async function POST(request:Request){try{const body=schema.parse(await request.json());const rates=await getShippingRates(body);return NextResponse.json({rates,demo:!process.env.BITESHIP_API_KEY})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Permintaan tidak valid"},{status:400})}}
