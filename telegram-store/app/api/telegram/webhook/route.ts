import{NextResponse}from"next/server";import{sendTelegramMessage}from"@/lib/telegram";export async function POST(request:Request){const update=await request.json();const message=update.message;if(message?.text==="/start"){const appUrl=process.env.APP_URL||"https://example.com";await fetch(`https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/sendMessage`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({chat_id:message.chat.id,text:"Selamat datang di Kriuk & Teguk 🍘🥤\nPilih snack dan minuman favoritmu langsung dari Telegram.",reply_markup:{inline_keyboard:[[{text:"🛍 Buka Toko",web_app:{url:appUrl}}],[{text:"📦 Pesanan Saya",callback_data:"orders"}]]}})})}else if(message?.chat?.id){await sendTelegramMessage(message.chat.id,"Ketik /start untuk membuka toko.")}return NextResponse.json({ok:true})}
